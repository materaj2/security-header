<?xml version="1.0" encoding="UTF-8"?>
<!--
===========================================================================
Apache Tomcat - Security Headers & Secure Cookie Configuration

This file contains configurations for:
  1. web.xml - Servlet filter and secure cookie settings
  2. server.xml - Connector and host settings
  3. context.xml - Cookie processor settings

Apply these settings to your Tomcat installation:
  - Global: $CATALINA_HOME/conf/web.xml and server.xml
  - Per-app: WEB-INF/web.xml
===========================================================================
-->

<!--
===========================================================================
PART 1: web.xml Configuration
Add these entries to your WEB-INF/web.xml or $CATALINA_HOME/conf/web.xml
===========================================================================
-->

<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
                             http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <!-- =================================================================
         1. Security Headers Filter (Tomcat built-in HttpHeaderSecurityFilter)
         Available since Tomcat 8.x
         ================================================================= -->
    <filter>
        <filter-name>httpHeaderSecurity</filter-name>
        <filter-class>org.apache.catalina.filters.HttpHeaderSecurityFilter</filter-class>

        <!-- Strict-Transport-Security -->
        <init-param>
            <param-name>hstsEnabled</param-name>
            <param-value>true</param-value>
        </init-param>
        <init-param>
            <param-name>hstsMaxAgeSeconds</param-name>
            <param-value>31536000</param-value>
        </init-param>
        <init-param>
            <param-name>hstsIncludeSubDomains</param-name>
            <param-value>true</param-value>
        </init-param>
        <init-param>
            <param-name>hstsPreload</param-name>
            <param-value>true</param-value>
        </init-param>

        <!-- X-Frame-Options -->
        <init-param>
            <param-name>antiClickJackingEnabled</param-name>
            <param-value>true</param-value>
        </init-param>
        <init-param>
            <param-name>antiClickJackingOption</param-name>
            <param-value>DENY</param-value>
        </init-param>

        <!-- X-Content-Type-Options -->
        <init-param>
            <param-name>blockContentTypeSniffingEnabled</param-name>
            <param-value>true</param-value>
        </init-param>

        <!-- X-XSS-Protection -->
        <init-param>
            <param-name>xssProtectionEnabled</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>

    <filter-mapping>
        <filter-name>httpHeaderSecurity</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- =================================================================
         2. Custom Security Headers Filter
         For headers not covered by HttpHeaderSecurityFilter.
         Deploy as a servlet filter in your application.
         ================================================================= -->
    <!--
    Create this Java class: com.example.security.SecurityHeadersFilter

    package com.example.security;

    import javax.servlet.*;
    import javax.servlet.http.HttpServletResponse;
    import java.io.IOException;

    public class SecurityHeadersFilter implements Filter {

        @Override
        public void init(FilterConfig filterConfig) throws ServletException {}

        @Override
        public void doFilter(ServletRequest request, ServletResponse response,
                             FilterChain chain) throws IOException, ServletException {

            HttpServletResponse httpResponse = (HttpServletResponse) response;

            // Content-Security-Policy
            httpResponse.setHeader("Content-Security-Policy",
                "default-src 'self'; " +
                "script-src 'self'; " +
                "style-src 'self' 'unsafe-inline'; " +
                "img-src 'self' data: https:; " +
                "font-src 'self'; " +
                "connect-src 'self'; " +
                "object-src 'none'; " +
                "frame-src 'none'; " +
                "frame-ancestors 'none'; " +
                "base-uri 'self'; " +
                "form-action 'self'; " +
                "upgrade-insecure-requests");

            // Referrer-Policy
            httpResponse.setHeader("Referrer-Policy",
                "strict-origin-when-cross-origin");

            // Permissions-Policy
            httpResponse.setHeader("Permissions-Policy",
                "accelerometer=(), camera=(), geolocation=(), " +
                "gyroscope=(), magnetometer=(), microphone=(), " +
                "payment=(), usb=()");

            // Cross-Origin-Opener-Policy
            httpResponse.setHeader("Cross-Origin-Opener-Policy",
                "same-origin");

            // Cross-Origin-Resource-Policy
            httpResponse.setHeader("Cross-Origin-Resource-Policy",
                "same-origin");

            // Cross-Origin-Embedder-Policy
            httpResponse.setHeader("Cross-Origin-Embedder-Policy",
                "require-corp");

            // Cache-Control
            httpResponse.setHeader("Cache-Control",
                "no-store, no-cache, must-revalidate, proxy-revalidate");
            httpResponse.setHeader("Pragma", "no-cache");
            httpResponse.setHeader("Expires", "0");

            chain.doFilter(request, response);
        }

        @Override
        public void destroy() {}
    }
    -->

    <!-- Register the custom filter -->
    <!--
    <filter>
        <filter-name>SecurityHeadersFilter</filter-name>
        <filter-class>com.example.security.SecurityHeadersFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>SecurityHeadersFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>
    -->

    <!-- =================================================================
         3. Secure Session/Cookie Configuration
         ================================================================= -->

    <!-- Force session cookies to be secure and HttpOnly -->
    <session-config>
        <!-- Session timeout in minutes -->
        <session-timeout>30</session-timeout>

        <cookie-config>
            <!-- Cookie name -->
            <name>JSESSIONID</name>

            <!-- HttpOnly: prevent JavaScript access -->
            <http-only>true</http-only>

            <!-- Secure: only send over HTTPS -->
            <secure>true</secure>

            <!-- Path scope -->
            <path>/</path>

            <!-- Max age in seconds (-1 = session cookie) -->
            <max-age>1800</max-age>

            <!-- SameSite attribute (Tomcat 9.0.29+) -->
            <!-- Configured via CookieProcessor in context.xml (see Part 3) -->
        </cookie-config>

        <!-- Use HTTPS-only session tracking -->
        <tracking-mode>COOKIE</tracking-mode>
    </session-config>

    <!-- =================================================================
         4. Error Pages (prevent information leakage)
         ================================================================= -->
    <error-page>
        <error-code>400</error-code>
        <location>/error/400.html</location>
    </error-page>
    <error-page>
        <error-code>401</error-code>
        <location>/error/401.html</location>
    </error-page>
    <error-page>
        <error-code>403</error-code>
        <location>/error/403.html</location>
    </error-page>
    <error-page>
        <error-code>404</error-code>
        <location>/error/404.html</location>
    </error-page>
    <error-page>
        <error-code>500</error-code>
        <location>/error/500.html</location>
    </error-page>
    <error-page>
        <exception-type>java.lang.Throwable</exception-type>
        <location>/error/500.html</location>
    </error-page>

    <!-- =================================================================
         5. Security Constraints (force HTTPS)
         ================================================================= -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Entire Application</web-resource-name>
            <url-pattern>/*</url-pattern>
        </web-resource-collection>
        <user-data-constraint>
            <transport-guarantee>CONFIDENTIAL</transport-guarantee>
        </user-data-constraint>
    </security-constraint>

</web-app>

<!--
===========================================================================
PART 2: server.xml Configuration Snippets
Add/modify in $CATALINA_HOME/conf/server.xml
===========================================================================

    *** HTTPS Connector with secure settings ***

    <Connector port="8443"
               protocol="org.apache.coyote.http11.Http11NioProtocol"
               maxThreads="150"
               SSLEnabled="true"
               scheme="https"
               secure="true"
               server=" "
               xpoweredBy="false">
        <SSLHostConfig
            protocols="TLSv1.2+TLSv1.3"
            ciphers="TLS_AES_256_GCM_SHA384:TLS_AES_128_GCM_SHA256:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-GCM-SHA256"
            honorCipherOrder="true">
            <Certificate
                certificateKeystoreFile="conf/keystore.jks"
                certificateKeystorePassword="changeit"
                type="RSA" />
        </SSLHostConfig>
    </Connector>

    *** Hide server information ***

    <Connector port="8080"
               protocol="HTTP/1.1"
               server=" "
               xpoweredBy="false" />

    *** Disable shutdown port in production ***

    <Server port="-1" shutdown="SHUTDOWN">
-->

<!--
===========================================================================
PART 3: context.xml Configuration
Add to $CATALINA_HOME/conf/context.xml or META-INF/context.xml
===========================================================================

    *** SameSite Cookie Configuration (Tomcat 9.0.29+) ***

    <Context>
        <CookieProcessor
            className="org.apache.tomcat.util.http.Rfc6265CookieProcessor"
            sameSiteCookies="Strict" />
    </Context>

    *** Disable session persistence across restarts ***

    <Context>
        <Manager pathname="" />
    </Context>
-->
